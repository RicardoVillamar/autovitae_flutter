import 'package:flutter/material.dart';

class CardVehicle extends StatelessWidget {
  const CardVehicle({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
